const Joi = require('..');


try {
	Joi.assert('test', Joi.string().min(10));
} catch (error) {
	console.log(error);
}

try {
	Joi.assert('12345', [Joi.string().min(10), Joi.number()]);
} catch (error) {
	console.log(error);
}
